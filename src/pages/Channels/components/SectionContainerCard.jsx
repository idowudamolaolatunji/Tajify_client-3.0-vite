import React from 'react';

function SectionContainerCard ({ children }) {

    return (
        <div className="section__container main__section">
            {children}
        </div>
    );
}

export default SectionContainerCard;